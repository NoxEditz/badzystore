---
paths:
  - "src/api/**"
  - "server/**"
---
# Backend Guidelines
- Validate all external input before use
- Return consistent error shapes across endpoints
- Log errors with context, never swallow exceptions silently
